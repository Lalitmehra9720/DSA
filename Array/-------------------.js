// Find the pair of elements whose sum is equal to a given number.
let arr = [1,4,2,5,3]
FindPair=(collections,target)=>{
    for(let i = 0; i < collections.length; i++){
        while(collections[i] < target){
            
        }
    }
}
let number = 10
let pairs = FindPair(arr, number)
console.log(`the pair of elements whose sum is equal to ${number} is : ${pairs}`)